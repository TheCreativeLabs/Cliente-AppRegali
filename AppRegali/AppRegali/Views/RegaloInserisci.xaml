<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppRegali.Views.RegaloInserisci">
    <ContentPage.Content>
        <StackLayout>
            <ScrollView>
                <StackLayout Margin="15" Padding="15">
                    <Frame CornerRadius="10"  HeightRequest="200" IsClippedToBounds="True" Padding="0" HasShadow="True" Visual="Material">
                        <Image x:Name="imgRegaloModifica" Source="{Binding Item.ImmagineRegalo, Converter={StaticResource ByteArrayToImage}}" Aspect="AspectFill"></Image>
                    </Frame>
                    <Button Style="{x:StaticResource PhotoButton}">
                        <!--Clicked="OnPickPhotoButtonClicked"-->
                    </Button>

                    <Entry x:Name="entTitoloRegalo" Visual="Material" Placeholder="{Binding RegaloInserisci.Titolo}" Text="{Binding Item.Titolo}"></Entry>

                    <Entry x:Name="entPrezzoRegalo" Visual="Material" Placeholder="{Binding RegaloInserisci.Prezzo}" Text="{Binding Item.Prezzo}"></Entry>

                    <Entry x:Name="entDescrizioneRegalo" Visual="Material" Placeholder="{Binding RegaloInserisci.Descrizione}" Text="{Binding Item.Descrizione}"></Entry>

                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>