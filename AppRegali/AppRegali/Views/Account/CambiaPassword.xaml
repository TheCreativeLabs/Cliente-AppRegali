<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:translator="clr-namespace:AppRegali.Helpers"
             mc:Ignorable="d"
             x:Class="AppRegali.Views.Login.CambiaPassword">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Margin="30">
            <Label Text="{translator:Translate ChangePassword.InsertNewPassword}"></Label>

            <Entry x:Name="entVecchiaPassword" Visual="Material" Placeholder="{translator:Translate ChangePassword.OldPassword}">
            </Entry>

            <Entry x:Name="entNuovaPassword" Visual="Material" Placeholder="{translator:Translate ChangePassword.NewPassword}">
            </Entry>

            <Entry x:Name="entConfermaPassword" Visual="Material" Placeholder="{translator:Translate ChangePassword.ConfirmNewPassword}">
            </Entry>

            <Button Visual="Material" Text="{translator:Translate ChangePassword.Continue}" x:Name="btnProcedi" Clicked="btnProcedi_Clicked"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>