<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:translator="clr-namespace:AppRegali.Helpers"
             xmlns:local="clr-namespace:AppRegali.Render"
             mc:Ignorable="d"
             x:Class="AppRegali.Views.Login.Login"
             NavigationPage.HasNavigationBar="False"
             >
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Margin="10">
            <Label FontSize="90" HorizontalTextAlignment="Center" FontFamily="{x:StaticResource FA-Solid}" TextColor="#39C4EF" FontAttributes="Bold" Margin="0,0,0,10" Text="&#xf4be;"></Label>

            <Label Text="{translator:Translate Login.ErroreLogin}" FontSize="Medium" TextColor="Red" IsVisible="False" x:Name="lblValidazioneLogin"></Label>
            
            <Entry Style="{x:StaticResource MaterialEntry}" x:Name="entUsername" Visual="Material" Placeholder="{translator:Translate Login.Username}"></Entry>
            <Label Text="{translator:Translate Login.MessaggioUsernameObbligatorio}" FontSize="Small" TextColor="Red" IsVisible="False" x:Name="lblValidatorUsername"></Label>

            <Entry Style="{x:StaticResource MaterialEntry}" Visual="Material" x:Name="entPassword" Placeholder="{translator:Translate Login.Password}" IsPassword="True"></Entry>
            <Label Text="{translator:Translate Login.MessaggioPasswordObbligatoria}" FontSize="Small" TextColor="Red" IsVisible="False" x:Name="lblValidatorPassword"></Label>

            <Button Style="{x:StaticResource primaryButton}"  Visual="Material" Text="{translator:Translate Login.SignIn}" x:Name="btnAccedi" Clicked="btnAccedi_ClickedAsync"></Button>

            <Label Text="{translator:Translate Login.ForgotPassword}" HorizontalTextAlignment="End" TextColor="black">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                </Label.GestureRecognizers>
            </Label>

            <Label Text="{translator:Translate Login.Or}" HorizontalTextAlignment="Center" Margin="0,10,0,10"></Label>

            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" >
                <Label FontFamily="{x:StaticResource FA-Brands}" Text="&#xf09a;" TextColor="#1e73be" ></Label>
                <Label Text="{translator:Translate Login.FacebookSignIn}" TextColor="#1e73be">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="btnAccediFacebook_Clicked"/>
                    </Label.GestureRecognizers>
                </Label>
            </StackLayout>


            <Label x:Name="lblRegistrati" Text="{translator:Translate Login.RegisterIfNoAccount}" HorizontalTextAlignment="Center" Margin="0,10,0,0" TextColor="black">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_lblRegistrati"/>
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>